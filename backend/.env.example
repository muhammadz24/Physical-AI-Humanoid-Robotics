# ==============================================================================
# Physical AI & Humanoid Robotics - Backend Environment Variables
# ==============================================================================
# Copy this file to .env and fill in your actual credentials
# DO NOT commit .env to version control (already in .gitignore)
# ==============================================================================

# ------------------------------------------------------------------------------
# FastAPI Configuration
# ------------------------------------------------------------------------------
ENVIRONMENT=development
DEBUG=True
API_HOST=0.0.0.0
API_PORT=8000
API_VERSION=1.0.0

# ------------------------------------------------------------------------------
# Qdrant Vector Database (Cloud)
# ------------------------------------------------------------------------------
QDRANT_URL=https://your-cluster-url.gcp.cloud.qdrant.io
QDRANT_API_KEY=your-qdrant-api-key-here
QDRANT_COLLECTION=textbook_embeddings

# ------------------------------------------------------------------------------
# Neon Postgres Database
# ------------------------------------------------------------------------------
# Format: postgresql://user:password@host:port/database
# Example: postgresql://user:pass@ep-cool-darkness-123456.us-east-2.aws.neon.tech/neondb
DATABASE_URL=postgresql://user:password@host:5432/database

# ------------------------------------------------------------------------------
# CORS Configuration (Production Origin) - Constitution Principle IX
# ------------------------------------------------------------------------------
# Localhost origins (http://localhost:3000, http://127.0.0.1:3000) are hardcoded in main.py
# Add your production frontend URL here (comma-separated for multiple origins)
# Local Development: http://localhost:3000
# Production Example: https://yourapp.vercel.app,https://yourdomain.com
ALLOWED_ORIGINS=http://localhost:3000

# ------------------------------------------------------------------------------
# Embeddings Configuration
# ------------------------------------------------------------------------------
EMBEDDING_MODEL=sentence-transformers/all-MiniLM-L6-v2
EMBEDDING_DIMENSION=384

# ------------------------------------------------------------------------------
# Google Gemini Configuration (Free Tier)
# ------------------------------------------------------------------------------
GEMINI_API_KEY=your-gemini-api-key-here
GEMINI_MODEL=gemini-1.5-flash

# ------------------------------------------------------------------------------
# JWT Authentication
# ------------------------------------------------------------------------------
# Generate a secure random string: openssl rand -hex 32
JWT_SECRET_KEY=your-jwt-secret-key-here-min-32-chars

# ------------------------------------------------------------------------------
# Rate Limiting
# ------------------------------------------------------------------------------
RATE_LIMIT_PER_MINUTE=10

# ==============================================================================
# DEPLOYMENT INSTRUCTIONS (Constitution Principle IX: Zero-Edit Deployment)
# ==============================================================================
#
# LOCAL DEVELOPMENT:
# 1. Copy this file to .env in backend/ directory: cp .env.example .env
# 2. Fill in your actual API keys and database URLs
# 3. Run: python -m uvicorn main:app --reload
#
# VERCEL PRODUCTION DEPLOYMENT:
# 1. Push code to GitHub repository
# 2. Connect repository to Vercel
# 3. Go to: Vercel Dashboard > Your Project > Settings > Environment Variables
# 4. Add each variable above with production values:
#    - DATABASE_URL: Your Neon Postgres production connection string
#    - QDRANT_URL: Your Qdrant Cloud production URL
#    - QDRANT_API_KEY: Your Qdrant Cloud API key
#    - GEMINI_API_KEY: Your Google Gemini API key
#    - JWT_SECRET_KEY: Strong random string (32+ chars)
#    - ALLOWED_ORIGINS: Your production frontend URL (e.g., https://yourapp.vercel.app)
#    - ENVIRONMENT: production
#    - DEBUG: False
# 5. Deploy: vercel --prod
# 6. No code changes required between environments!
#
# SECURITY REMINDERS:
# - Never commit .env files to version control
# - Use different API keys for development and production
# - Rotate secrets regularly (JWT_SECRET_KEY, API keys)
# - In production: Set ENVIRONMENT=production and DEBUG=False
# - Verify ALLOWED_ORIGINS matches your deployed frontend URL exactly
# ==============================================================================
